framework:
    http_client:
        scoped_clients:
            devevents.client:
                base_uri: 'https://www.devevents-api.fr'
                headers:
                    Accept: 'application/json'
                    apikey: '%env(DEVEVENTS_API_KEY)%'
