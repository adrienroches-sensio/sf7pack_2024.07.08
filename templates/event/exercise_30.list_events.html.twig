{# TODO: Use this template for your page's body #}

<section id="list-events">
    <div class="container">
        <div class="row">
            <div class="col-md-10 m-auto">
                <h1 class="fw-bolder" style="color: limegreen;">List all events</h1>
                <div class="row row-cols-1 row-cols-md-2 g-4 mb-5">
                    {# TODO: Loop here #}
                        <div class="col">
                            <div class="card h-100">
                                <div class="card-body">
                                    <div class="card-title">
                                        <h5 class="fw-bolder mb-0">
                                            <a href="{# TODO: Replace me #}"
                                               class="stretched-link text-decoration-none"
                                               style="color: limegreen;">{# TODO: Replace me #}</a>
                                        </h5>
                                    </div>
                                    <div class="card-text d-flex justify-content-between">
                                        <div>
                                            {# TODO: Condition here #}
                                                <p class="my-0">{# TODO: Replace me #}</p>
                                            {# TODO: End condition #}
                                            {# TODO: Condition here #}
                                                <p class="my-0">{# TODO: Replace me #}</p>
                                            {# TODO: End condition #}
                                        </div>
                                        <div>
                                            <p class="mb-0">From <span
                                                        class="fw-semibold">{# TODO: Replace me #}</span>
                                                to
                                                <span class="fw-semibold">{# TODO: Replace me #}</span></p>
                                            <p class="my-0 fst-italic">{# TODO: Replace me #} volunteers
                                                yet.</p>
                                            <p class="mt-0">Accessible
                                                : {# TODO: Replace me #}</p>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>
                    {# TODO: else Loop #}
                        <div> There are no events yet...</div>
                    {# TODO: end Loop #}
                </div>
            </div>
        </div>
    </div>
</section>
